export const metadata = {
  title: "Sign Up",
  description: "Create a Vehiculars account to easily process vehicle licences, number plates, and road worthiness online.",
};

export default function SignupLayout({ children }) {
  return <>{children}</>;
}
