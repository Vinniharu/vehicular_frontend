export const metadata = {
  title: "Reset Password",
  description: "Reset your Vehiculars account password to regain access to your vehicle compliance dashboard.",
};

export default function ResetPasswordLayout({ children }) {
  return <>{children}</>;
}
