export const metadata = {
  title: "Log In",
  description: "Log in to your Vehiculars account to manage your vehicle compliance, track applications, and view documents.",
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
